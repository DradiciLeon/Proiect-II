<UserControl x:Class="Activity_Finder.SupportControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="ContactTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2D3436"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="15,0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- BARA DE SUS -->
        <Border Grid.Row="0" Background="#CCFFFFFF" Padding="20,15" Margin="20,15,20,10" CornerRadius="20">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🎧 SUPPORT" FontSize="20" FontWeight="Black" Foreground="#2D3436" VerticalAlignment="Center" Margin="0,0,30,0"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📞 +40 7xx xxx xxx" Style="{StaticResource ContactTextStyle}"/>
                        <Separator Width="1" Height="20" Background="#B2BEC3"/>
                        <TextBlock Text="✉️ support@hobbyapp.com" Style="{StaticResource ContactTextStyle}"/>
                    </StackPanel>
                </StackPanel>

                <Button x:Name="BtnBack" Content="BACK" Click="BtnBack_Click" HorizontalAlignment="Right" Width="80" Height="35" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#2D3436" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="White" TextBlock.FontWeight="Bold"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- CONTINUT -->
        <Grid Grid.Row="1">
            <Border Background="#CCFFFFFF" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="50" CornerRadius="30" MaxWidth="600">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="How can we help you?" FontSize="28" FontWeight="Black" Foreground="#2D3436" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                    <TextBlock Text="Our team is available Monday to Friday, 09:00 - 18:00." Foreground="#636E72" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                    <TextBlock Text="Message us directly:" FontWeight="Bold" Foreground="#2D3436" Margin="0,0,0,10"/>
                    <Border Background="White" CornerRadius="15" Padding="5" Margin="0,0,0,20">
                        <TextBox x:Name="TxtSupportMessage" Height="150" BorderThickness="0" Padding="10" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>
                    </Border>

                    <Button Content="SEND MESSAGE" Click="BtnSendMessage_Click" Height="50" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="25">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#FF6B6B" Offset="0"/>
                                            <GradientStop Color="#FF9E5E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="White" TextBlock.FontWeight="Black"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>