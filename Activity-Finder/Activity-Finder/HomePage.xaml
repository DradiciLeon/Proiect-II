<Window x:Class="Activity_Finder.HomePage"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Activity_Finder"
    mc:Ignorable="d"
    Title="HomePage" Height="600" Width="900" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="HoverButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="BigCardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="ExtraBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" Opacity="0.15" ShadowDepth="4" Color="Gray"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="253*"/>
            <ColumnDefinition Width="647*"/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF9E5E" Offset="0.0" />
                <GradientStop Color="#FEB47B" Offset="0.4" />
                <GradientStop Color="#FF7EB3" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#CCFFFFFF" Padding="15" Margin="15,15,15,15" CornerRadius="20" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="18" MaxWidth="350" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" Margin="15,0">
                        <TextBlock Text="🔍" VerticalAlignment="Center" Foreground="Gray"/>
                        <TextBox Background="Transparent" BorderThickness="0" Padding="10" Width="250" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Home" Margin="5,0" Style="{StaticResource HoverButtonStyle}"/>
                    <Button Content="Profil" Margin="5,0" Style="{StaticResource HoverButtonStyle}"/>
                    <Button Content="Setări" Margin="5,0" Style="{StaticResource HoverButtonStyle}"/>
                    <Button Content="Support" Margin="5,0" Style="{StaticResource HoverButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <UniformGrid Grid.Row="1" Columns="3" Margin="20,20,20,20" Grid.ColumnSpan="2">
            <Button Content="CATEGORIES" 
            Style="{StaticResource BigCardButtonStyle}" 
            Click="Categories_Click"/>

            <Button Content="POST A HOBBY" 
            Style="{StaticResource BigCardButtonStyle}" 
            Click="PostHobby_Click"/>

            <Button Content="NEW CATEGORY" 
            Style="{StaticResource BigCardButtonStyle}"/>
        </UniformGrid>

    </Grid>
</Window>
